/**
 * Formatting utilities — re-exports from localeFormat for path alias compatibility.
 */
export { formatCurrency, formatDate, formatTime, formatDateTime, formatWeight, formatDimensions } from '@/utils/localeFormat';
